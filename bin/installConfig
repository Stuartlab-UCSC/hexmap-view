#!/bin/bash
# Reinstall the config and bin.

$HEXMAP/bin/checkConfig.sh
if [ "$?" == 1 ]; then
    exit 1
fi
source $HEX_VIEWER_CONFIG

cd $HEXMAP
rm -rf bin config
tar -xf /cluster/home/swat/dev/config.tar
