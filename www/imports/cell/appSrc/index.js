
// A stub so the cell page may be developed independent of the tumor map
// application.

import React from 'react';
import ReactDOM from 'react-dom';
import Cell from './Cell.js'

ReactDOM.render(
  <Cell />,
  document.getElementById('root')
);
